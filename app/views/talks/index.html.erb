<div class="text-center">
  <% @talks.each do |talk| %>
    <div>
      <p><%= talk.title %></p>
      <iframe width="560" height="315" src="<%= talk.url %>" frameborder="0" allow="accelerometer; autoplay; encrypted-media; gyroscope; picture-in-picture" afullscreen></iframe>
    </div>
  <% end %>
</div>